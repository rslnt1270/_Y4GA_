<!-- © YAGA Project — Todos los derechos reservados -->

# Reporte Semanal Y4GA — Semana del 2026-04-13 al 2026-04-19 — Sprint #5 (inicio)

## 1. Resumen Ejecutivo

Durante la semana del 13 al 19 de abril de 2026, el equipo de YAGA no registró commits de código funcional. La única actividad en el repositorio fue la incorporación del reporte ejecutivo automatizado correspondiente a la semana anterior (Sprint 4). Esta semana corresponde al período de transición entre el cierre del Sprint 4 —que entregó cuatro correcciones de seguridad críticas, recuperación de contraseña y correcciones en Poleana— y el arranque formal del Sprint 5. Los conductores continúan utilizando la plataforma con todas las funcionalidades de los sprints previos: registro de viajes por voz, dashboard de jornada, comparativa histórica y GPS cifrado.

---

## 2. Estado de la Aplicación

| Parámetro | Valor |
|-----------|-------|
| Versión | 0.5.0 (FastAPI app) |
| Infraestructura | AWS EC2 t3.small + Docker Compose |
| Backend | FastAPI Python 3.11 + asyncpg + Redis 7 |
| Base de datos | PostgreSQL 16 |
| Frontend | PWA HTML monolítico (~2,650 líneas, vanilla JS/CSS) |
| Métricas de DB | No disponible en reporte automático — consultar EC2 manualmente |

> Para verificar estado en producción: `curl -s http://localhost:8000/health` desde el container EC2.

---

## 3. Métricas de Uso

| Métrica | Valor | Fuente |
|---------|-------|--------|
| Conductores activos | No disponible — requiere acceso EC2 | — |
| Viajes registrados | No disponible — requiere acceso EC2 | — |
| Comandos de voz procesados | No disponible — requiere acceso EC2 | — |
| Viajes históricos en BD | 3,299 (importados Jun 2024–Feb 2026) | CLAUDE.md |

> Para obtener métricas reales:
> ```bash
> ssh EC2 'docker exec yaga_postgres psql -U yaga_user -d yaga_db -c "SELECT COUNT(*) FROM usuarios; SELECT COUNT(*) FROM jornadas; SELECT COUNT(*) FROM viajes;"'
> ```

---

## 4. Actividad de la Semana

| Actividad | Commit | Tipo | Detalle |
|-----------|--------|------|---------|
| Reporte ejecutivo semana anterior | `b93c98b` | docs | Incorporación del reporte semanal 2026-04-06 al repositorio |

> **Nota:** No se registraron commits de código funcional durante esta semana. El único cambio fue la adición automática del reporte ejecutivo previo.

---

## 5. Calidad Técnica

- **Archivos Python modificados:** 0
- **Commits realizados:** 1 (solo documentación)
- **Bugs corregidos:** Ninguno esta semana
- **Deuda técnica pendiente (backlog de seguridad):**
  - Vuln. 6.5: Poleana sigue siendo parcialmente client-authoritative — cliente envía estado completo; el servidor debe ser única fuente de verdad
  - Vuln. 6.6: `_rooms` dict en memoria se pierde al reiniciar el servidor (migrar a Redis)
  - Vuln. 6.7: Rate limiting (slowapi) no implementado en `main.py`
  - Sistema B legacy (`app/routers/`, `app/core/auth.py`) pendiente de eliminación definitiva

---

## 6. Seguridad y Compliance

| Control | Estado | Detalle |
|---------|--------|---------|
| Cifrado PII (AES-256-GCM) | ✅ Activo | `app/core/crypto.py` — IV único por registro, prohibido pgp_sym_encrypt |
| Auth JWT HS256 (Sistema A) | ✅ Activo | Único sistema activo; Sistema B deshabilitado desde Sprint 4 |
| JWT en memoria (frontend) | ✅ Activo | `_authToken` en variable JS — prohibido localStorage/sessionStorage |
| Reset de contraseña | ✅ Activo | Redis TTL 3600s, token de un solo uso |
| Rate limiting | ⚠️ Pendiente | slowapi no implementado — Sprint 5 (Vuln. 6.7) |
| Refresh tokens | ⚠️ Roadmap | No implementados — tokens de sesión únicos |
| Auditoría ARCO | ⚠️ Pendiente | Tabla `auditoria` existe; reimplementar logging en Sistema A |
| Derechos ARCO (LFPDPPP) | ✅ Rutas activas | GET acceso, PUT rectificación, POST cancelación, POST oposición |
| Soft delete usuarios | ✅ Activo | Campo `deleted_at` en tabla `usuarios` |

---

## 7. Próximos Hitos — Sprint 5

| Hito | Prioridad | Descripción |
|------|-----------|-------------|
| Rate limiting (slowapi) | 🔴 Alta | Protección anti-abuso en todos los endpoints (Vuln. 6.7) |
| Poleana server-authoritative | 🔴 Alta | Servidor como única fuente de verdad; cliente solo envía intenciones ROLL/MOVE |
| Poleana rooms en Redis | 🟠 Media | Persistir `_rooms` en Redis para sobrevivir reinicios (Vuln. 6.6) |
| Eliminar Sistema B legacy | 🟠 Media | Borrar `app/routers/` y `app/core/auth.py` tras validar ausencia de dependencias |
| Auditoría ARCO en Sistema A | 🟡 Media | Registrar acciones `arco_*` en tabla `auditoria` con IP y user_agent |
| Refresh tokens | 🟡 Baja | Mejorar experiencia de sesión sin comprometer seguridad |

---

## 8. Riesgos

| Riesgo | Impacto | Probabilidad | Mitigación |
|--------|---------|--------------|------------|
| Sin rate limiting en endpoints | Alto — DoS, abuso de NLP/auth | Media | Implementar slowapi en Sprint 5 como primera tarea (Vuln. 6.7) |
| Poleana client-authoritative | Medio — trampas, inconsistencia de estado | Media | Migrar lógica al servidor en Sprint 5 (Vuln. 6.5) |
| Rooms de Poleana en memoria | Medio — pérdida de partidas en reinicio | Baja | Migrar `_rooms` a Redis en Sprint 5 (Vuln. 6.6) |
| Sistema B legacy en codebase | Bajo — confusión, posible reactivación accidental | Baja | Eliminar archivos en Sprint 5 tras validación con grep |
| Auditoría ARCO incompleta | Medio — riesgo de compliance LFPDPPP | Media | Reimplementar logging ARCO en Sistema A en Sprint 5 |
| Semana sin commits de código | Bajo — velocidad de entrega reducida | N/A | Retomar cadencia de commits en Sprint 5 |

---

## 9. Recomendaciones

1. **Arrancar Sprint 5 con rate limiting como primera entrega.** El endpoint `/api/v1/auth/login` y `/api/v1/command` (NLP por voz) operan sin ninguna restricción de frecuencia en producción. Agregar `slowapi` con límites conservadores (10 req/min en login, 30 req/min en command) es la corrección de mayor impacto en seguridad disponible y puede completarse en pocas horas.

2. **Formalizar el kickoff del Sprint 5 con fechas y responsables.** La semana sin actividad de código sugiere que el equipo está en transición. Definir explícitamente el alcance del Sprint 5 (al menos las vulnerabilidades 6.5–6.7 y eliminación de Sistema B) y asignar responsables concretos evitará que la deuda técnica acumulada siga creciendo.

3. **Habilitar métricas automáticas desde producción.** Este es el segundo reporte consecutivo sin datos de conductores activos ni viajes registrados. Se recomienda exponer un endpoint interno `/metrics` (protegido por red interna o token de servicio) que retorne contadores básicos de la BD, permitiendo que los próximos reportes incluyan indicadores de negocio reales.

---

*Reporte generado automáticamente el 2026-04-20 por YAGA Report Bot.*
*Datos de BD no disponibles en ambiente de CI — requieren acceso SSH al EC2 en producción.*
