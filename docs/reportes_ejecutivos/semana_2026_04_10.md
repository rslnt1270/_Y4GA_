# Reporte Ejecutivo — Semana del 2026-04-04 al 2026-04-10
**YAGA Project · Sprint 6 + 7 + 8**
Branch: `claude/gps-module-planning-Rb6UF`
EC2: `ec2-3-19-35-76.us-east-2.compute.amazonaws.com` · Stack: Docker Compose (postgres16 + valkey7.2 + FastAPI)

---

## 1. Resumen ejecutivo

Se cerraron **3 sprints consecutivos** con enfoque en compliance LFPDPPP, hardening P0 y migración de Poleana a arquitectura server-authoritative. El sistema ahora cumple con los derechos ARCO end-to-end (backend + frontend), elimina credenciales hardcodeadas, migra de Redis SSPL a Valkey BSD, y reduce drásticamente la superficie de ataque pública al cerrar puertos 5432/6379 y ocultar Swagger en producción.

**Estado del servicio:** operativo, 3 contenedores healthy, sin downtime no planeado. La ventana de mantenimiento del 10-abr duró ~2 minutos (rebuild + restart).

---

## 2. Commits de la semana

| SHA | Título | Alcance |
|---|---|---|
| `49e9547` | security: Sprint 6+7 — hardening P0 + GPS privacy | Valkey, JWT_SECRET obligatorio, DB_ENCRYPT_KEY obligatoria, GPS lat/lng truncado a 4 decimales (~11m), AUTHORSHIP.md |
| `d435ac9` | feat: Sprint 6 W1-W2 — logging JSON + ARCO + audit + asyncpg | logging estructurado, particiones GPS idempotentes, audit_service fire-and-forget, ARCO 4 endpoints, consentimientos, dependencies→asyncpg |
| `a9b46a6` | feat: Sprint 6 W3 — Poleana server-authoritative (fix vuln 6.5) | poleana_engine_adapter, WS rewrite (STATE/GAME_OVER cliente→ERROR), cliente JS migrado a sendMove() |
| `1fbe3ab` | feat: Sprint 6 W4 — NLP 13 intents + panel ARCO frontend | intent_catalog expandido, classifier scoring fix, panel ARCO con 4 secciones y 248 líneas de CSS/JS |

4 commits, ~2,400 líneas netas.

---

## 3. Sprint 6 — LFPDPPP + Observabilidad + Poleana

### W1: Infraestructura de observabilidad
- `app/core/logging.py` — `JSONFormatter` estructurado (timestamp UTC ISO, level, module, exc_info)
- `docker-compose.yml` logging driver `json-file` con rotación (50m × 3)
- `app/scripts/gps_partitions.py` — creación idempotente de particiones `jornada_gps_logs_YYYY_MM`
- `app/scripts/gps_retention.py` — drop particiones antiguas con `dry_run=True` por defecto
- `app/dependencies.py` — migrado SQLAlchemy → asyncpg (elimina dependencia Sistema B)

### W2: ARCO compliance (LFPDPPP)
- `app/services/audit_service.py` — `log_action()` fire-and-forget con `asyncio.create_task`, cast `$3::inet`, sanitización IP vacía
- `app/api/v1/auth.py` — helpers `_client_ip`/`_client_ua`, audit en login/register/reset
- `app/services/arco_service.py` — `get_datos_acceso`, `rectificar_datos`, `cancelar_cuenta` (soft-delete + anonimización, phone=NULL), `gestionar_oposicion`
- `app/api/v1/arco.py` — 4 endpoints con rate limit 5/min
- `app/api/v1/consentimientos.py` — GET list + POST upsert

**Fix crítico:** cancelación pone `phone=NULL` para evitar colisión con UNIQUE cuando se cancelan múltiples cuentas.
**Fix:** removido `from __future__ import annotations` de routers para evitar `PydanticUserError` en Pydantic v2.

### W3: Poleana server-authoritative (vuln 6.5)
- `app/services/poleana_engine_adapter.py` (~430 líneas) — `PoleanaGameState` serializable con value-tracking 0-56 (sin BFS por tablero), `apply_roll`, `apply_move`
- `app/api/v1/poleana.py` — WS rewrite: STATE/GAME_OVER desde cliente → `ERROR`. Cliente solo envía `ROLL`/`MOVE`; servidor persiste en Valkey
- `Poleana_Project/web/poleana_engine.js` — `sendOnlineState()` deshabilitado, `sendMove(move)` nuevo, handlers `MOVE_RESULT`/`GAME_OVER`

**Impacto:** el cliente ya no puede inyectar estados ganadores ni modificar posiciones de piezas. Servidor es única fuente de verdad.

### W4: NLP expansion + panel ARCO frontend
- `intent_catalog.py` — +13 intents (EN_CAMINO_RECOGER, INICIAR_VIAJE, TERMINAR_VIAJE, TERMINAR_JORNADA, AGREGAR_EFECTIVO/TARJETA/GASOLINA/COMIDA/PEAJE/ESTACIONAMIENTO/GASTO_GENERAL, COMANDO_INCOMPLETO). `IntentPattern` extendido con `required_none`, `requires_amount`
- `classifier.py` — scoring con `specific_single`: keywords ≥4 chars single-word obtienen min 0.6. Fix: "gasolina 300" y "caseta 45" antes caían en UNKNOWN
- `frontend/index.html` +248 líneas — panel ARCO con 4 secciones (mis datos, rectificar, consentimientos, eliminar cuenta)

---

## 4. Sprint 7 — Hardening P0 + GPS Privacy

### 7-A: Seguridad de configuración
- `docker-compose.yml` — Redis → Valkey 7.2-alpine (BSD 3-Clause, elimina riesgo SSPL y CVE-2025-49844)
- `--requirepass ${VALKEY_PASSWORD}` obligatorio
- Puertos `5432` y `6379` sin binding al host
- `app/core/config.py` — `REDIS_URL` apunta a valkey, elimina `JWT_ALGORITHM` RS256 inconsistente
- `app/services/database.py` — elimina credencial hardcodeada `Yaga2026_Secure#Pass`
- `app/core/crypto.py` — `RuntimeError` en producción si `DB_ENCRYPT_KEY` no configurada
- `app/api/v1/auth.py` — consolida cliente Redis compartido (`core.redis`)
- `app/requirements.txt` — pin `starlette>=0.49.1` (fix CVE-2025-62727 DoS)
- `.env.example` — documenta todas las variables requeridas

### 7-B: Privacidad GPS
- `app/services/gps_service.py` — `_truncar_coordenada()` reduce precisión a 4 decimales (~11m) al descifrar
- `app/services/historico_service.py` — `round(lat,4)/round(lng,4)` en `get_mapa_data()`

### IP: AUTHORSHIP.md
Registro INDAUTOR, protocolo de commits con IA, roadmap IMPI.

---

## 5. Sprint 8 — Consent granular + cierre de gaps

### W1: Migración 005 consentimientos granulares ✅
`infrastructure/database/migrations/005_consentimientos_granulares.sql` aplicado en prod:
- `+canal TEXT` (general/email/push/sms/whatsapp)
- `+fecha_revocacion TIMESTAMPTZ`
- `UNIQUE(usuario_id, finalidad, canal)` reemplaza constraint previa
- Índice parcial `idx_consentimientos_revocacion WHERE fecha_revocacion IS NOT NULL`

### W2: Modal consentimiento granular (frontend) ✅
`frontend/index.html` — `<div id="consentOverlay">` con 3 filas (Operación disabled+checked, Marketing, Investigación). JS: `abrirModalConsentimientos()`, `guardarConsentimientos()` (diff + POST por finalidad cambiada), hook en `doRegister()` para mostrarlo post-registro. Desplegado en EC2 (3330 líneas).

### W3: Auditoría post-merge + ventana de mantenimiento ✅
El security subagent detectó 5 hallazgos. Se resolvieron en ventana del 10-abr (duración ~2 min):

| # | Hallazgo | Estado |
|---|---|---|
| 1 🔴 | Puertos 5432/6379 expuestos 0.0.0.0 | ✅ Cerrados tras rebuild con nuevo compose |
| 2 🔴 | `JWT_SECRET` ausente (tokens con secret efímero) | ✅ Generado 64 hex chars, persistido en `.env` |
| 3 🟠 | `/api/docs` público | ✅ `docs_url=None` cuando `ENVIRONMENT=production` |
| 4 🟠 | `yaga_api` unhealthy (curl no existe en imagen) | ✅ Healthcheck migrado a `python3 urllib.request` |
| 5 🟡 | Contenedor `yaga_redis` seguía activo | ✅ Eliminado como orphan, reemplazado por `yaga_valkey` |

**Sistema B residual** (`SECRET_KEY`, `JWT_PRIVATE_KEY`, `JWT_PUBLIC_KEY`, `JWT_ALGORITHM`, `AES_256_KEY_HEX`) eliminado del `.env` de EC2. `DB_ENCRYPT_KEY` duplicada consolidada al valor activo (hash `f277647e...`), preservando legibilidad de PII cifrada existente.

### W4: Este reporte ✅

---

## 6. Estado del servicio (post-mantenimiento)

```
NAMES           STATUS                    PORTS
yaga_api        Up (healthy)              0.0.0.0:8000->8000/tcp
yaga_postgres   Up (healthy)              5432/tcp (solo red interna)
yaga_valkey     Up (healthy)              6379/tcp (solo red interna)
```

| Control | Resultado |
|---|---|
| `GET /health` | 200 |
| `GET /api/docs` | 404 (prod) |
| `GET /api/v1/arco/acceso` sin token | 401 |
| `GET /api/v1/consentimientos` sin token | 401 |
| Rate limit `/api/v1/auth/login` | req6=429 (5/min activo) |
| Logs JSON estructurados | ✓ módulo `yaga.main` |
| Sin stack traces expuestos | ✓ |
| ENVIRONMENT=production | ✓ |

---

## 7. Deuda técnica pendiente

| Prioridad | Item | Sprint destino |
|---|---|---|
| 🟠 | Nginx + Cloudflare frente a EC2 (TLS terminación, WAF) | Sprint 9 |
| 🟡 | Auditoría en Sistema A — tabla `auditoria` existe pero sólo Sistema B (deshabilitado) la usaba; `audit_service` ya implementado, falta cablear en endpoints ARCO y NLP | Sprint 9 |
| 🟡 | Eliminar archivos `app/routers/` y `app/core/auth.py` (Sistema B legacy) tras validar que nada los importa | Sprint 9 |
| 🟡 | Refresh tokens (roadmap histórico) | Sprint 10 |
| 🟢 | Copiar `Poleana_Project/engine/*.txt` al container para habilitar torres/puentes en el engine completo | Backlog |
| 🟢 | Migración a EKS vía External Secrets Operator | Roadmap |

---

## 8. Métricas operativas

- **Conductores activos (histórico):** 14
- **Viajes importados (jun 2024–feb 2026):** 3,299
- **Ticket promedio histórico:** ~$72.93 MXN
- **Uptime EC2 t3.small:** 100% durante la semana (1 restart planificado)
- **Tests:** no configurados (backlog)

---

## 9. Próximos pasos inmediatos

1. **Sprint 9** — Nginx+Cloudflare frente a EC2 + cableado de `audit_service` en endpoints ARCO/NLP.
2. **Deploy Cloudflare Pages** — subir `Poleana_Project/web/poleana_engine.js` migrado a server-authoritative.
3. **Validación UX** — verificar manualmente el modal de consent granular con un nuevo registro.

---

*Generado automáticamente el 2026-04-10 por el pipeline de reportes ejecutivos Y4GA.*
